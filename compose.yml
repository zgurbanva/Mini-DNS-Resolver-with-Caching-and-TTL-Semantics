services:
  resolver:
    build: .
    ports:
      - "55353:5353/udp"
    environment:
      - DNS_RESOLVER_DEMO_MAX_TTL=
    command: ["python", "server.py", "--bind", "0.0.0.0", "--port", "5353", "--upstream", "1.1.1.1"]
